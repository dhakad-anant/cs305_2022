<queries>
    <!--<sql id="allAboutActor" paramType="org.foo.Bar">
        <![CDATA[
        SELECT * FROM actor WHERE last_name=${propX};
        ]]>
    </sql>-->
    <sql id="passingObject" paramType="assignment_1.parameter1_int">
         <![CDATA[
         select * from actor where actor_id=${propX};
         ]]>
    </sql>

    <sql id="passingPrimitive" paramType="java.lang.Integer">
        <![CDATA[
         select * from actor where actor_id=${propX};
         ]]>
    </sql>

    <!--<sql id="addMovie" paramType="org.foo.Bar">
        <![CDATA[
        INSERT INTO my_table(x, y, x) VALUES(${propX}, ${propY}, ${propZ});
        ]]>
    </sql>
     <sql id="findMovies" paramType="org.foo.Bar">
        <![CDATA[
        SELECT actor_id,last_name FROM actor WHERE last_name=${propX};
        ]]>
    </sql>
    <sql id="delete_film_actor" paramType="org.foo.Bar">
        <![CDATA[
        DELETE FROM film_actor WHERE actor_id=${propX} and film_id=${propY};
        ]]>
    </sql>
    <sql id="insert_film_actor" paramType="org.foo.Bar">
        <![CDATA[
        INSERT INTO film_actor (actor_id, film_id, last_update) VALUES (${propX}, ${propY}, ${propZ});
        ]]>
    </sql>-->
</queries>